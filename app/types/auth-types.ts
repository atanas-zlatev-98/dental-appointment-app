export type SignUpFormData = {
    name: string;
    email: string;
    phone: string;
    password: string;
    profilePictureUrl: string;
}

export type SignInFormData = {
    email: string;
    password: string;
}